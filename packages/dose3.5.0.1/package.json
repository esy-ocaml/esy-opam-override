{
  "build": [
    [
      "broke",
      "-c",
      "#{os == 'windows' ? 'patch -p1 < _esy/dose3-5.0.1.patch' : 'true'}"
    ],
    [
      "./configure"
    ],
    [
      "make",
      "printconf"
    ],
    [
      "bash",
      "-c",
      "#{os == 'windows' ? 'make' : 'make libs'}"
    ]
  ]
}
