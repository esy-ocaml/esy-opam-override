{
  "build": [
    [
      "bash",
      "-c",
      "patch -p1 < _esy/merlin-20170712.patch"
    ],
    [
      "make",
      "-f",
      "Makefile",
      "PREFIX=#{self.install}",
      "USE_OCAMLFIND=true",
      "docdir=#{self.doc}/menhir",
      "libdir=#{self.lib}/menhir",
      "mandir=#{self.man}/man1"
    ]
  ]
}
