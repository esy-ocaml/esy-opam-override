{
  "build": [
    "#{os == 'windows' ? 'dos2unix ./winconfigure' : 'bash -c true'}",
    [
      "bash",
      "-c",
      "#{os == 'windows' ? './winconfigure' : 'true' }"
    ],
    [
      "jbuilder",
      "build",
      "-p",
      "sexplib",
      "-j",
      "4"
    ]
  ]
}
